---
name: codecov

on:
  workflow_run:
    workflows:
      - test
    types:
      - completed

jobs:
  on_success:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - name: "foo 0"
        run: ls -lhta
        shell: "bash"
      - name: "foo 1"
        run: ls -lhta _reports/
        shell: "bash"
...
